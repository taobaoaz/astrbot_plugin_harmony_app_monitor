name: harmony_app_monitor
version: '1.0.0'
description: 监控华为应用市场鸿蒙应用更新并自动推送
author: YourName
license: MIT

# 插件依赖的Python库，AstrBot会在加载插件时检查
dependencies:
  - playwright>=1.40.0
  - beautifulsoup4>=4.12.0
  - requests>=2.31.0
  - apscheduler>=3.10.0
  
config:
  apps_to_watch:
    type: list
    default:
      - app_name: "一日记账"
        detail_url: "https://appgallery.huawei.com/app/detail?id=com.ericple.onebill"
        version_selector: "span.content-value"
    description: |
      需要监控的应用列表，格式：
      - app_name: "应用显示名"
        detail_url: "应用市场详情页URL"
        version_selector: "CSS选择器"

  check_interval_minutes:
    type: integer
    default: 30
    min: 5
    max: 1440
    description: 检查更新的时间间隔（分钟）

events: []
commands: []
